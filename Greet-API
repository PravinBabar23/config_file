# For Admin Server
  boot:
    admin:
      client:
        url: http://localhost:1111/

  # For Zipkin tracing (ensure your app sends data via another mechanism if not using Sleuth)
  zipkin:
    base-url: http://localhost:9411.  

